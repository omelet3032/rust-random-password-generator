**4. 보안 관점**`rand::rng()` (구 `ThreadRng`)은 암호학적으로 안전한 CSPRNG이므로 패스워드 생성 용도로 적합합니다. 다만 실제 패스워드 생성기라면 다음도 고려해볼 만합니다:

- 최소 길이 검증 (`length == 0`일 때 빈 문자열 반환됨 — 의도한 동작인지 확인)
- 문자 종류 다양성 보장(대문자/소문자/숫자/특수문자 각 최소 1개 포함) — 무작위 샘플링만으로는 우연히 한 종류만 나올 수도 있음
- 특정 문자(예: 스크립트 인젝션에 취약한 `'`, `"`, ``` 등)를 제외하고 싶다면 범위 대신 화이트리스트 방식 고려

##